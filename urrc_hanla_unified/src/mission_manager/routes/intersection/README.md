# Vehicle intersection routes

Only GPS trajectories recorded on the actual course belong here. Do not place
generated or synthetic coordinates in this directory. Expected basenames are
`{N,E,S,W}_{STRAIGHT,LEFT,RIGHT}` with matching `.csv` and `.yaml` files.

